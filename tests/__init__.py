"""Top-level test package.

- tests/integration/  — end-to-end pipeline + API contract tests.
- tests/fixtures/     — shared JSON fixtures (sample profiles, sample KB).
Per-engine unit tests live inside each engine's own tests/ subfolder.
"""
