"""Tiny shared helpers — keep this package small on purpose.

If a helper is only used by one engine, it belongs inside that engine's folder.
Promote to utils/ only when at least two engines need it.
"""
